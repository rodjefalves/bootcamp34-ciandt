#Profile
spring.profiles.active=dev

# Springfox uses path matching based on AntPathMatcher, while Spring Boot 2.6.X uses PathPatternMatcher
# This setting equates the two options.
springdoc.api-docs.path=/api-brewery